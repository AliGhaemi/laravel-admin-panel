import { type BreadcrumbItem } from '@/types';

export default function Header() {
    return (
        <header className="w-full fixed h-20 top-0 dark:bg-secondary border-b dark:border-b-hover z-20">

        </header>
    );
}
